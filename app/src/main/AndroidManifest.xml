<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE" />

    <uses-feature android:name="android.software.companion_device_setup" />

    <application
        android:name=".BluetoothBouncerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BluetoothBouncer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BluetoothBouncer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Shizuku provider for permission management -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Boot receiver to re-apply policies after reboot -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Bond state receiver to handle device re-pairing -->
        <receiver
            android:name=".receivers.BondStateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.BOND_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Temporary-allow notification action receiver -->
        <receiver
            android:name=".receivers.TemporaryAllowReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="net.harveywilliams.bluetoothbouncer.ACTION_ALLOW_TEMPORARILY" />
            </intent-filter>
        </receiver>

        <!-- CompanionDeviceService for background Bluetooth presence detection (API 33+) -->
        <service
            android:name=".service.DeviceWatcherService"
            android:exported="true"
            android:permission="android.permission.BIND_COMPANION_DEVICE_SERVICE">
            <intent-filter>
                <action android:name="android.companion.CompanionDeviceService" />
            </intent-filter>
        </service>

    </application>

</manifest>
